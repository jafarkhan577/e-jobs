specVersion: "v1beta"
serviceId: "job_portal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "job_portal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
